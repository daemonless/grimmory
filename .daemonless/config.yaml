icon: ':material-bookshelf:'

cit:
  compose: true
  mode: screenshot
  port: 6060
  wait: 120
  health: /api/v1/healthcheck
  screenshot_wait: 30
  # screenshot drifts ~0.94x from lazy load, loosen the gate.
  ssim_threshold: 0.92
